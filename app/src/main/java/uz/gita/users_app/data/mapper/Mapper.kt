package uz.gita.users_app.data.mapperimport uz.gita.users_app.data.model.UsersModelimport uz.gita.users_app.data.source.remote.response.Result//fun UsersResponse.toModel(): UsersModel = UsersModel(//    fullName = "${this.results.map { it.name.title }}" + "${this.results.map { it.name.first }}" + "${this.results.map { it.name.last }}",//    gender = this.results.map { it.gender }.toString(),//    country = "${this.results.map { it.location.country }}," + "${this.results.map { it.location.city }}",//    email = this.results.map { it.email }.toString(),//    picture = this.results.map { it.picture.medium }.toString()//)//fun Result.toModel(): UsersModel = UsersModel(//    fullName = "${this.name.title} ${this.name.first} ${this.name.last}",//    gender = this.gender,//    address = "${this.location.country} ${this.location.city}",//    email = this.email,//    picture = this.picture.medium//)fun Result.toModel(): UsersModel = UsersModel(    fullName = this.name.title + this.name.first + this.name.last,    gender = this.gender,    country = this.location.country,    email = this.email,    picture = this.picture.medium)